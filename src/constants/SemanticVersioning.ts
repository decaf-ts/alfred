/**
 * @summary holds the values for the semantic versioning system (patch | minor | major)
 * @description holds the values for the semantic versioning system (patch | minor | major)
 * @const SEMANTIC_VERSIONING
 * @memberOf module:alfred
 */
export const SEMANTIC_VERSIONING = {
  PATCH: "patch",
  MINOR: "minor",
  MAJOR: "major",
} as const;
